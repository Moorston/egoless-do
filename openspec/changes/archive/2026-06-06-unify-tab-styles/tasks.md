## 1. 修改 MindTrailTab.tsx 样式

- [x] 1.1 修改 `apps/mobile/src/features/reflections/MindTrailTab.tsx` 的 tab 样式
  - 背景：选中 `${P}20` → `P`，未选中 `transparent` → `TH.card`
  - 边框：移除 `borderWidth` 和 `borderColor`
  - 文字颜色：选中 `P` → `#fff`，未选中保持 `TH.sub`
  - 字重：选中 `600` → `700`，未选中 `400` → `500`
  - 圆角：`10` → `12`
  - 字体：`FONT_SMALL` → `FONT_BODY`

## 2. 修改 MindTrailScreen.tsx 样式

- [x] 2.1 修改 `apps/mobile/src/features/reflections/MindTrailScreen.tsx` 的 tab 样式
  - 同 1.1 的样式变更
