# 安全审计报告

> 审计日期: 2026-07-29
> 范围: apps/mobile + backend/pb_hooks + packages/core

## 依赖漏洞（pnpm audit）

| 数量 | 严重度 | 主要包 |
|------|--------|--------|
| 7 | high | brace-expansion, fast-uri, postcss |
| 2 | moderate | uuid, node-tar |

**性质**: 全部为 Expo/Sentry 传递依赖，非直接依赖。
**修复**: 跟随 Expo SDK / Sentry 升级自动解决。可通过 `pnpm overrides` 临时 pin。

## 代码安全

| 检查项 | 结果 |
|--------|------|
| 硬编码密钥 | ✅ 未发现 |
| SQL 注入 | ✅ 全参数化查询 |
| 认证绕过 | ✅ JWT + ownership 校验 |
| XSS | ✅ React Native 默认转义 |
| 路径遍历 | ✅ 无用户输入拼路径 |

## 建议

1. **依赖升级**: 下次 Expo SDK 升级时同步解决 9 个漏洞
2. **pnpm overrides**: 对 tar/uuid 加临时 pin（如需紧急修复）
3. **CI 集成**: 加 `pnpm audit` 到 CI pipeline（阻断 high/critical）

## 结论

**整体安全状况良好**。无直接可利用漏洞，依赖漏洞为行业常见问题（Expo 生态）。
