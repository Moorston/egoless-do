import { describe, it, expect, vi, beforeEach } from 'vitest';
import { create{{properCase name}}Slice } from './create{{properCase name}}Slice';

// ── Mocks ──
const mockAdapter = {
  persistChange: vi.fn().mockResolvedValue(undefined),
  markDeleted: vi.fn().mockResolvedValue(undefined),
};

const mockOnSync = vi.fn();

describe('{{properCase name}}Slice', () => {
  let slice: ReturnType<typeof create{{properCase name}}Slice>;

  beforeEach(() => {
    slice = create{{properCase name}}Slice(mockAdapter, mockOnSync);
    vi.clearAllMocks();
  });

  it('should initialize with empty list', () => {
    expect(slice.{{camelCase name}}s).toEqual([]);
  });

  it('should add item with generated id and timestamps', () => {
    slice.add{{properCase name}}({ name: 'Test' });
    expect(mockOnSync).toHaveBeenCalled();
  });

  it('should update item by id', () => {
    // TODO: Add test
  });

  it('should delete item by id (soft delete)', () => {
    // TODO: Add test
  });
});
